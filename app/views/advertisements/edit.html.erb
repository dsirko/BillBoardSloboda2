<h1>Edit Advertisement</h1>

<%= form_for :advertisement, url: advertisement_path(@advertisement), method: :patch do |f| %>

  <% if @advertisement.errors.any? %>
    <dif id="error_explanation">
      <h2>
        <%= pluralize(@advertisement.errors.count, "error") %> prohibited
        this advertisement from being saved:
      </h2>
      <ul>
        <% @advertisement.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<br/>
<%= link_to 'Back', advertisements_path %>
